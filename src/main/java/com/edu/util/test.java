package com.edu.util;

import java.io.File;
import java.io.IOException;

public class test {

    public static final String saveRoute = "D:\\saveDoc";

    public static void main(String[] args) {
        /*// 将文件保存在该目录下
        File file = new File(saveRoute);
        // 如果不存在，创建该目录
        if (!file.exists()){
            file.mkdir();
        }
        File newFile = new File(file,"newname");
        try {
            // 将文件写入到磁盘中
            File myFile = new File("F:\\boy.jpg");
            myFile.transferTo(newFile);
        }catch (IOException e){

        }*/
    }
}
